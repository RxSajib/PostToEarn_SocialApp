<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/colorPrimary"
    android:layout_height="match_parent"
    tools:context=".ForgotPasswordActivity">


    <ImageView
        android:id="@+id/coufisedconID"
        android:layout_width="120dp"
        android:src="@drawable/confused"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:layout_height="120dp">
    </ImageView>

    <TextView
        android:id="@+id/ConfusedTextID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/coufisedconID"
        android:layout_marginTop="10dp"
        android:textSize="30sp"
        android:textColor="@android:color/white"
        android:layout_centerHorizontal="true"
        android:text="Forgot Password?"
        />

    <TextView
        android:id="@+id/child"
        android:layout_below="@+id/ConfusedTextID"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="5dp"
        android:textColor="@android:color/white"
        android:textSize="17dp"
        android:text="Forgot Password don't worry just put your email address and done"
        android:layout_marginRight="20dp"
        android:layout_marginLeft="20dp"
        android:gravity="center"
        android:layout_height="wrap_content"/>


    <EditText
        android:id="@+id/FPasswordID"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/child"
        android:layout_marginTop="50dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:textColor="@android:color/white"
        android:textColorHint="#F1F1F1"
        android:backgroundTint="#6f00c5"
        android:singleLine="true"
        android:padding="10dp"
        android:inputType="textEmailAddress"
        android:hint="Enter Your Email"
        />

    <Button
        android:id="@+id/RestPasswordButtonID"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/FPasswordID"
        android:layout_marginRight="25dp"
        android:background="@drawable/google"
        android:layout_marginLeft="25dp"
        android:layout_marginTop="10dp"
        android:textColor="@android:color/white"
        android:textSize="17sp"
        android:text="Reset Password"
        />

</RelativeLayout>